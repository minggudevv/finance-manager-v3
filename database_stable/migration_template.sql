-- Database Migration Template
-- Use this template when creating new migration files
-- Filename should follow format: from_vX.Y.Z_to_vA.B.C.sql

/*
Migration Plan:
- Version from: v0.0.0
- Version to: v0.0.0
- Description: [Brief description of changes]
- Date: YYYY-MM-DD
- Author: [Your name]

Changes included:
1. [List of database changes]
2. 
3. 

Rollback plan:
1. [How to rollback these changes if needed]
2.
*/

-- 1. Add your migration SQL statements here:

-- Example migration:
-- ALTER TABLE table_name ADD COLUMN new_column TEXT;

-- 2. Add any data migration if needed:

-- Example data migration:
-- UPDATE table_name SET new_column = 'default_value' WHERE new_column IS NULL;

-- 3. Update any functions, views, or other database objects as needed:

-- 4. Add any new policies or permissions if needed:

-- 5. Test your migration with sample data before applying to production

/*
-- ROLLBACK SECTION (commented out, uncomment for actual rollback)
-- Use this section to rollback the migration if needed
-- Example:
-- ALTER TABLE table_name DROP COLUMN new_column;
*/